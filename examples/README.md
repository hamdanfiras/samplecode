# Examples

Complete examples live here, grouped by topic.

Use this pattern:

```text
examples/<category>/<technology>/<sample-name>/
```

Add a `README.md` inside each sample folder when the example needs setup or run instructions.
