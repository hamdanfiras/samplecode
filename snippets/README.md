# Snippets

Use this folder for small standalone code fragments that do not need a complete project structure.

If a snippet grows setup steps, dependencies, or multiple files, move it into `examples/`.
